# View: V_SST_IndicadorApl

```sql
CREATE VIEW [dbo].[V_SST_IndicadorApl]
AS
SELECT 'GTH' AS cod_apl, 'GESTIÓN HUMANA' AS des_apl
UNION
SELECT 'SST' AS cod_apl, 'SEGURIDAD Y SALUD EN EL TRABAJO' AS des_apl

```
